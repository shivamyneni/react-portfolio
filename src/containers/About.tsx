import React, { memo } from "react";

const About = memo(() => {
  return (
    <div className="w-screen h-screen bg-green-50  card rounded-t-[50px] AboutMainDiv fixed z-10"></div>
  );
});

export default About;
